/* WIP sketch — incomplete / not compiling.
 *
 * For a correct SharedPtr implementation with tests, see:
 *   ../c++/shared_ptr.cpp
 *
 * Exam points this draft was aiming at:
 * - RAII, Rule of Five
 * - Heap refcount shared across copies
 * - Copy vs move; self-assignment
 * - Prefer std::atomic for thread-safe counts (this draft is unfinished)
 */
